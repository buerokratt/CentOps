export interface Client {
  id: string;
  name: string;
}

export interface ClientSecret {
  id: string;
  name: string;
}

export interface ClientManifest {
  id: string;
  name: string;
  updatedAt: string;
}
