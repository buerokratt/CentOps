apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: database
  ports:
  - protocol: TCP
    port: {{ .Values.ports.database }}
    targetPort: 5432
  type: ClusterIP