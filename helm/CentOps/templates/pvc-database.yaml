apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.volumes.database.storage }}