declaration:
  call: declare
  version: 0.1
  description: "Delete a client"
  method: delete
  accepts: json
  returns: json
  namespace: centops
  endpoint: /clients/{name}
  allowlist:
    params:
      - field: clientId
        type: string
    headers:
      - field: cookie
        type: string
        description: "Cookie field"

extract_request_data:
  assign:
    client_id: ${incoming.params.clientId}
  next: validate_input

validate_input:
  switch:
    - condition: ${client_id == null || incoming.headers.cookie == null}
      next: return_bad_request
  next: call_create_user_log_template

call_create_user_log_template:
  template: "[#CENTOPS_PROJECT_LAYER]/log-user-request"
  requestType: templates
  body:
    method: "DELETE"
    path: "admin/clients"
  headers:
    cookie: ${incoming.headers.cookie}
  result: log_result
  next: delete_client

delete_client:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]/delete-client"
    body:
      client_id: ${incoming.params.clientId}
  result: delete_result
  next: return_deleted_client

return_deleted_client:
  status: 204
  return:
  next: end

return_bad_request:
  status: 400
  return: "Client ID and cookie are required"
  next: end