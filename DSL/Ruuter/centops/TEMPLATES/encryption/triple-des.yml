declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'triple-des'"
  method: post
  accepts: json
  returns: json
  namespace: centops
  allowlist:
    body:
      - field: content
        type: string
        description: "Body field 'content'"
      - field: key
        type: string
        description: "Body field 'key'"

triple_des_encryption:
  call: http.post
  args:
    url: "[#CENTOPS_DMAPPER]/encryption/triple-des"
    body:
      content: ${incoming.body.content}
      key: ${incoming.body.key}
  result: result

return_value:
  return: ${result.response.body.cipher}
  next: end
