{
"items": [
{{#each entries}}
    {
    "clusterId": "{{clusterId}}",
    "name": "{{name}}",
    "ipAddress": "{{ipAddress}}",
    "createdAt": "{{createdAt}}"
    }{{#unless @last}},{{/unless}}
{{/each}}
],
"page": {{page}},
"pageSize": {{pageSize}},
"totalPages": {{#if entries.[0]}}{{entries.[0].totalPages}}{{else}}0{{/if}}
}