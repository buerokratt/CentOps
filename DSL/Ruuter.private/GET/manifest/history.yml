get_manifests_history:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]:[#CENTOPS_RESQL_PORT]/get-manifests-all-history"
  result: results

return_value:
   wrapper: false
   return: ${results.response.body}
