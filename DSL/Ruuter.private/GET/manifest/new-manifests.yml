get_approved_manifests:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]:[#CENTOPS_RESQL_PORT]/get-manifests-pending-approval"
  result: results

return_value:
   wrapper: false
   return: ${results.response.body}
