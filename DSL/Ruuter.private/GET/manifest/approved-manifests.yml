get_approved_manifests:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]:[#CENTOPS_RESQL_PORT]/get-approved-manifests"
  result: results

return_value:
   wrapper: false
   return: ${results.response.body}
