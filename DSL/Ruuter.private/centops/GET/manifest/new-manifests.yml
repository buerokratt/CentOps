declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'new-manifests'"
  method: get
  accepts: json
  returns: json
  namespace: centops

get_approved_manifests:
  call: http.post
  args:
    url: "[#CENTOPS_RESQL]/get-manifests-pending-approval"
  result: results
  next: return_value

return_value:
   wrapper: false
   return: ${results.response.body}
   next: end
